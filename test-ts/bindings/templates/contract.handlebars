import { TransactionObject, Tx } from "web3/types";
import { BN } from "bn.js";

// tslint:disable:max-line-length
export interface {{contractName}}Contract {
{{#each methods}}
    {{#this.constant}}
    {{this.tsName}}({{> typed_params inputs=inputs}}): Promise<{{> return_type outputs=outputs}}>;
    {{/this.constant}}
    {{^this.constant}}
    {{this.tsName}}({{> typed_params inputs=inputs}}): TransactionObject<void>;
    {{/this.constant}}
{{/each}}
    address: string;
}
// tslint:enable:max-line-length
